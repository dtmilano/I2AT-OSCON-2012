<manifest package="com.dtmilano.i2at.tc"
    android:versionCode="15"
    android:versionName="1.5" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >

        <!-- If this is included and a menu item defines an onClick in XML the app cannot be started -->
        <!-- android:theme="@style/AppTheme" -->

        <activity
            android:name=".TemperatureConverterActivity"
            android:label="@string/title_activity_temperature_converter" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="SettingsActivity"></activity>
    </application>

</manifest>